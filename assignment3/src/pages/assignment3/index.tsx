import { Container, Stack, Box, Typography } from "@mui/material";

// implement search functionality
// add icon to MF text search
// MF search shows no results before the user has searched. Initial page should contain search instructions
// MF search: search should search results. I'm feeling lucky should open the 1st result
// MF info: implement the page. fetch data+loading state+correct typescript types
// MF info: do basic analysis to recommend the fund or not
// MF info: use Data Grid component https://mui.com/x/react-data-grid/ instead of simple table

const Assignment3 = () => {
  return (
    <Container>
      <Stack textAlign="center">
        <Typography variant="h2">Assignment 3</Typography>
      </Stack>
    </Container>
  );
};

export default Assignment3;
