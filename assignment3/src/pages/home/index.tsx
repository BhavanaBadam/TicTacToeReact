import {Stack, Typography} from '@mui/material';
const Home = () => {
  return <Stack textAlign="center">
    <Typography variant="h1">Welcome</Typography>
    <Typography>Select an assignment from the sidebar on the left to get started </Typography>
  </Stack>;
}

export default Home;
